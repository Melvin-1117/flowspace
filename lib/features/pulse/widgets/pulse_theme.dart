import 'package:flutter/material.dart';
import '../../../app/theme.dart';

const pulseBackground = Color(0xFF000000);
const Color pulseCard = AppTheme.surfaceCard;
const Color pulsePrimary = AppTheme.primary;
const Color pulseCyan = AppTheme.accent;
const Color pulseText = AppTheme.textPrimary;
const Color pulseMuted = AppTheme.textSecondary;
const Color pulseSuccess = AppTheme.success;
const Color pulseDanger = AppTheme.danger;
