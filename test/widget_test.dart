import 'package:flutter_test/flutter_test.dart';
import 'package:flowspace/main.dart';

void main() {
  test('FlowSpaceApp widget can be instantiated', () {
    const app = FlowSpaceApp();
    expect(app, isA<FlowSpaceApp>());
  });
}
